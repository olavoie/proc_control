/*
 * Copyright 2015-2018 The MathWorks, Inc.
 */

try {
    project.catkin.tree.generateMessageArtifact(project, 'proc_control_matlab')
} catch (NullPointerException e) {
    println("Couldn't find proc_control_matlab on the ROS_PACKAGE_PATH")
}

task bugfixtask << {
    mkdir sourceSets.main.output.classesDir
}
jar.dependsOn(bugfixtask)
